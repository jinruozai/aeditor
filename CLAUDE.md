# aeditor — Claude Handoff

`AGENTS.md` is the current handoff authority for this repository. Read it first.

Use the current design documents under `doc/*.md`, starting with
`doc/README.md`. Do not implement from `doc/old/**` unless a current document
explicitly points there.

This file exists only as a compatibility pointer for Claude-based tools. It is
not a separate architecture document and must not be published as part of the
npm runtime package.
